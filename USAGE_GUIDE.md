# 📖 دليل الاستخدام - Piper TTS Platform

دليل شامل لاستخدام منصة تحويل النص إلى صوت Piper TTS.

## 🎯 الاستخدام الأساسي

### 1. البدء السريع

```bash
# استنساخ المستودع
git clone https://github.com/Motaart/tet.git
cd piper-tts-platform

# تثبيت التبعيات
pnpm install

# تشغيل خادم التطوير
pnpm run dev

# افتح المتصفح على http://localhost:5174
```

### 2. واجهة المستخدم

```
┌─────────────────────────────────────┐
│   🎙️ Piper TTS                      │
│   تحويل النص إلى صوت في المتصفح    │
├─────────────────────────────────────┤
│                                     │
│  النص المراد تحويله:                │
│  ┌─────────────────────────────────┐│
│  │ مرحبا بك في منصة تحويل النص... ││
│  └─────────────────────────────────┘│
│                                     │
│  السرعة: [===●====] 1.0x            │
│  المتحدث: [المتحدث الافتراضي ▼]    │
│                                     │
│  [🎵 تحويل إلى صوت] [⏹️ إيقاف]    │
│                                     │
│  ┌─────────────────────────────────┐│
│  │ ▶️ [====●==========]             ││
│  │ [⬇️ تحميل الملف]                ││
│  └─────────────────────────────────┘│
│                                     │
│  الحالة: 🔊 تشغيل  النموذج: LibriTTS│
└─────────────────────────────────────┘
```

## 🎵 خطوات الاستخدام

### الخطوة 1: إدخال النص

أدخل النص الذي تريد تحويله إلى صوت في حقل "النص المراد تحويله":

```
أمثلة:
- "مرحبا، كيف حالك؟"
- "هذا نص تجريبي لتحويل النص إلى صوت"
- "Piper TTS is a great text-to-speech engine"
```

### الخطوة 2: اختيار الإعدادات

#### السرعة (Speed)
- **0.5x**: أبطأ (مناسب للقراءة الدقيقة)
- **1.0x**: عادي (الافتراضي)
- **1.5x**: أسرع (مناسب للمراجعة السريعة)
- **2.0x**: سريع جداً (مناسب للاستعجال)

```typescript
// في الكود
const speed = 1.0  // القيمة الافتراضية
const length_scale = 1 / speed  // يتم حسابها تلقائياً
```

#### المتحدث (Speaker)
- **المتحدث الافتراضي**: الصوت الرئيسي
- يمكن إضافة متحدثين إضافيين من النموذج

```typescript
// في الكود
const speaker = 0  // رقم المتحدث
```

### الخطوة 3: تحويل النص

انقر على زر "🎵 تحويل إلى صوت":

```
الحالة: جاري المعالجة...
[🔄 جاري المعالجة...]
```

سيستغرق الأمر بضع ثوان حسب طول النص.

### الخطوة 4: تشغيل الصوت

بعد انتهاء المعالجة، سيظهر مشغل الصوت:

```
┌─────────────────────────────────────┐
│ ▶️ [====●==========] 00:05 / 00:15   │
│ [⬇️ تحميل الملف]                    │
└─────────────────────────────────────┘
```

- اضغط ▶️ للتشغيل
- اضغط ⏸️ للإيقاف المؤقت
- اسحب المؤشر للتنقل في الملف

### الخطوة 5: تحميل الملف

انقر على زر "⬇️ تحميل الملف" لحفظ الملف الصوتي:

```
الملف سيُحفظ باسم: tts-output-1705000000000.wav
```

## 🔧 الإعدادات المتقدمة

### تخصيص النموذج

عدّل ملف `src/main.ts`:

```typescript
// تغيير مصدر النموذج
this.state.modelUrl = 'https://your-custom-url/model.onnx'

// أو استخدام نموذج محلي
this.state.modelUrl = '/models/en_US-libritts-high.onnx'
```

### تغيير الإعدادات الافتراضية

```typescript
// الإعدادات الافتراضية
const DEFAULT_SPEED = 1.0
const DEFAULT_SPEAKER = 0
const DEFAULT_TEXT = 'مرحبا بك في منصة تحويل النص إلى صوت'

// عدّلها حسب احتياجاتك
```

### إضافة متحدثين إضافيين

```typescript
// في src/main.ts
const speakers = [
  { id: 0, name: 'المتحدث الافتراضي' },
  { id: 1, name: 'متحدث 2' },
  { id: 2, name: 'متحدث 3' },
]

// ثم أضفهم للـ select
speakers.forEach(speaker => {
  const option = document.createElement('option')
  option.value = speaker.id.toString()
  option.textContent = speaker.name
  speakerSelect.appendChild(option)
})
```

## 💻 الاستخدام البرمجي

### في React/TypeScript

```typescript
import { PiperTTS } from '@mintplex-labs/piper-tts-web'

// إنشاء instance
const piper = new PiperTTS()

// تحميل النموذج
await piper.loadModel('path/to/model.onnx')

// معالجة النص
const audioBuffer = await piper.synthesize('مرحبا', {
  speaker: 0,
  length_scale: 1.0,
})

// تشغيل الصوت
const audio = new Audio(URL.createObjectURL(
  new Blob([audioBuffer], { type: 'audio/wav' })
))
audio.play()
```

### في JavaScript العادي

```javascript
// تحميل المكتبة
import { PiperTTS } from '@mintplex-labs/piper-tts-web'

async function textToSpeech(text) {
  try {
    const piper = new PiperTTS()
    await piper.loadModel('model.onnx')
    
    const audio = await piper.synthesize(text, {
      speaker: 0,
      length_scale: 1.0,
    })
    
    const blob = new Blob([audio], { type: 'audio/wav' })
    const url = URL.createObjectURL(blob)
    
    const audioElement = new Audio(url)
    audioElement.play()
  } catch (error) {
    console.error('Error:', error)
  }
}

// الاستخدام
textToSpeech('مرحبا بالعالم')
```

## 🐛 استكشاف الأخطاء

### المشكلة: "جاري تحميل النموذج..." لا ينتهي

**الأسباب المحتملة**:
1. النموذج كبير جداً
2. اتصال الإنترنت بطيء
3. مشكلة في تحميل الملفات

**الحل**:
```bash
# تحقق من سرعة الإنترنت
# جرّب في متصفح مختلف
# امسح ذاكرة التخزين المؤقت
# Ctrl + Shift + Delete (Chrome)
```

### المشكلة: "خطأ: لا يمكن تحميل النموذج"

**الأسباب المحتملة**:
1. URL النموذج غير صحيح
2. مشكلة CORS
3. ملف النموذج تالف

**الحل**:
```bash
# تحقق من URL
curl -I https://your-model-url

# تحقق من CORS headers
curl -I -H "Origin: http://localhost:5174" https://your-model-url

# أعد تحميل النموذج
pnpm run download-model
```

### المشكلة: "خطأ في معالجة النص"

**الأسباب المحتملة**:
1. النص فارغ
2. النص يحتوي على أحرف غير مدعومة
3. مشكلة في النموذج

**الحل**:
```typescript
// تحقق من صحة النص
if (!text || text.trim().length === 0) {
  console.error('النص فارغ')
  return
}

// أزل الأحرف الخاصة
const cleanText = text.replace(/[^\w\s]/g, '')
```

### المشكلة: "الصوت لا يشغّل"

**الأسباب المحتملة**:
1. مشكلة في AudioContext
2. مشكلة في المتصفح
3. مشكلة في الملف الصوتي

**الحل**:
```typescript
// تحقق من AudioContext
if (audioContext.state === 'suspended') {
  audioContext.resume()
}

// جرّب متصفح مختلف
// تحقق من صوت النظام
```

## 📊 أمثلة الاستخدام

### مثال 1: تحويل نص بسيط

```
النص: "السلام عليكم"
السرعة: 1.0x
المتحدث: الافتراضي
النتيجة: ملف صوتي بصيغة WAV
```

### مثال 2: قراءة مقالة

```
1. انسخ نص المقالة
2. ألصقه في حقل النص
3. اختر سرعة 1.2x للقراءة السلسة
4. اضغط "تحويل إلى صوت"
5. استمع أو حمّل الملف
```

### مثال 3: إنشاء محتوى صوتي

```
1. اكتب النص المراد تحويله
2. جرّب سرعات مختلفة
3. اختر أفضل نسخة
4. حمّل الملف
5. استخدمه في مشروعك
```

## 🎓 نصائح وحيل

### نصيحة 1: تحسين جودة الصوت

- استخدم نصوص واضحة وسليمة
- تجنب الأحرف الخاصة غير الضرورية
- استخدم علامات الترقيم بشكل صحيح

```
❌ سيء: "مرحبا!!!؟؟؟ كيف حالك"
✅ جيد: "مرحبا. كيف حالك؟"
```

### نصيحة 2: تسريع المعالجة

- استخدم نصوص قصيرة
- قسّم النصوص الطويلة
- استخدم سرعة أعلى

```
❌ بطيء: نص بـ 1000 كلمة بسرعة 0.5x
✅ سريع: نص بـ 100 كلمة بسرعة 1.5x
```

### نصيحة 3: إدارة الملفات

- نظّم ملفاتك الصوتية
- استخدم أسماء واضحة
- احفظ نسخ احتياطية

```bash
# إنشاء مجلد للملفات الصوتية
mkdir -p ~/Audio/TTS
cd ~/Audio/TTS

# تنظيم الملفات
mv *.wav ./archive/
```

## 📱 الاستخدام على الهاتف

### متطلبات الهاتف

- متصفح حديث (Chrome, Firefox, Safari)
- اتصال إنترنت سريع
- ذاكرة كافية (500MB+)

### خطوات الاستخدام

1. افتح الرابط في المتصفح
2. انتظر تحميل النموذج
3. أدخل النص
4. اضغط "تحويل إلى صوت"
5. استمع أو حمّل

### نصائح الهاتف

- استخدم WiFi بدلاً من البيانات
- أغلق التطبيقات الأخرى
- استخدم نصوص قصيرة
- قم بتشغيل الصوت بصوت عالي

## 🔗 روابط مفيدة

- [الموقع الرسمي](https://github.com/Motaart/tet)
- [الإصدارات](https://github.com/Motaart/tet/releases)
- [المشاكل المعروفة](https://github.com/Motaart/tet/issues)
- [المساهمة](https://github.com/Motaart/tet/blob/main/CONTRIBUTING.md)

## 📞 الدعم

إذا واجهت مشاكل:

1. تحقق من [الأسئلة الشائعة](FAQ.md)
2. ابحث في [المشاكل السابقة](https://github.com/Motaart/tet/issues)
3. فتح [مشكلة جديدة](https://github.com/Motaart/tet/issues/new)

---

**ملاحظة**: هذا الدليل يتم تحديثه بانتظام. تأكد من مراجعة الإصدار الأخير.
