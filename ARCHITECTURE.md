# 🏗️ البنية المعمارية - Piper TTS Platform

## نظرة عامة

هذا المستند يشرح البنية التقنية للمشروع والحل الذي تم تطبيقه لمشكلة حجم الملفات الكبيرة على Cloudflare Workers.

## 📊 الرسم البياني المعماري

```
┌─────────────────────────────────────────────────────────────┐
│                      المتصفح (Browser)                      │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  React/TypeScript Application (src/main.ts)         │  │
│  │  - UI Components                                     │  │
│  │  - State Management                                  │  │
│  │  - Event Handlers                                    │  │
│  └──────────────────────────────────────────────────────┘  │
│                          ↓                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  ONNX Runtime Web                                    │  │
│  │  - Model Loading                                     │  │
│  │  - Inference Engine                                  │  │
│  └──────────────────────────────────────────────────────┘  │
│                          ↓                                   │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Piper TTS Web Library                               │  │
│  │  - Text Processing                                   │  │
│  │  - Audio Synthesis                                   │  │
│  │  - Voice Control                                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                    الخادم (Server)                           │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Cloudflare Workers (Optional)                       │  │
│  │  - Static Site Hosting                               │  │
│  │  - API Proxy                                         │  │
│  │  - CORS Handling                                     │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                  مصادر الملفات (Resources)                   │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │  GitHub Releases │  │  R2 / CDN        │                │
│  │  - Model Parts   │  │  - Model Parts   │                │
│  │  - Config Files  │  │  - Manifest      │                │
│  └──────────────────┘  └──────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 المكونات الرئيسية

### 1. واجهة المستخدم (UI Layer)

**الملف**: `src/main.ts`

```typescript
class PiperTTSApp {
  // إدارة الحالة
  private state: TTSState
  
  // تهيئة التطبيق
  private async init()
  
  // تحميل النموذج
  private async loadModel()
  
  // معالجة النص
  private async synthesize()
  
  // تشغيل الصوت
  private playAudio()
  
  // رسم الواجهة
  private render()
}
```

**المسؤوليات**:
- عرض واجهة المستخدم
- معالجة أحداث المستخدم
- إدارة حالة التطبيق
- تحديث الواجهة

### 2. محرك معالجة الصوت (Audio Engine)

**المكتبات المستخدمة**:
- `@mintplex-labs/piper-tts-web`: مكتبة Piper TTS
- `onnxruntime-web`: محرك ONNX للمتصفح

**العملية**:
```
النص → معالجة → ONNX Model → Audio Buffer → WAV File
```

### 3. نظام تحميل النموذج (Model Loading System)

**الملف**: `scripts/download-model.mjs`

**الميزات**:
- تحميل النموذج من GitHub Releases
- تقسيم الملفات الكبيرة
- إنشاء ملف Manifest
- التخزين المؤقت المحلي

## 🎯 حل مشكلة حجم الملفات

### المشكلة الأصلية

| المعامل | القيمة |
|--------|--------|
| حجم النموذج الأصلي | 137 MB |
| حد Cloudflare Workers | 25 MB |
| النسبة | 5.5x أكبر من الحد |

### الحل المطبق

#### 1️⃣ تقسيم الملفات (File Chunking)

```
en_US-libritts-high.onnx (137 MB)
        ↓
    تقسيم إلى
        ↓
┌─────────────────────────────────────┐
│ Part 0: 20 MB ✓ (< 25 MB)          │
│ Part 1: 20 MB ✓ (< 25 MB)          │
│ Part 2: 20 MB ✓ (< 25 MB)          │
│ Part 3: 20 MB ✓ (< 25 MB)          │
│ Part 4: 20 MB ✓ (< 25 MB)          │
│ Part 5: 17 MB ✓ (< 25 MB)          │
└─────────────────────────────────────┘
```

#### 2️⃣ استضافة خارجية (External Hosting)

```
Cloudflare Workers (25 MB limit)
        ↑
        │ يحمّل من
        ↓
GitHub Releases / R2 / CDN
(بدون حد للحجم)
```

#### 3️⃣ تحميل ديناميكي (Dynamic Loading)

```typescript
// في المتصفح
const chunks = [];
for (let i = 0; i < totalChunks; i++) {
  const chunk = await fetch(`/models/en_US-libritts-high.onnx.part${i}`);
  chunks.push(await chunk.arrayBuffer());
}

// دمج الأجزاء
const fullModel = Buffer.concat(chunks);
```

#### 4️⃣ ملف Manifest (Metadata)

```json
{
  "model": "en_US-libritts-high",
  "version": "1.0.0",
  "chunks": 6,
  "chunkSize": 20971520,
  "files": [
    "en_US-libritts-high.onnx.part0",
    "en_US-libritts-high.onnx.part1",
    "en_US-libritts-high.onnx.part2",
    "en_US-libritts-high.onnx.part3",
    "en_US-libritts-high.onnx.part4",
    "en_US-libritts-high.onnx.part5"
  ]
}
```

## 🔄 تدفق البيانات

### 1. تهيئة التطبيق

```
1. تحميل الصفحة
   ↓
2. تهيئة AudioContext
   ↓
3. إنشاء instance من PiperTTS
   ↓
4. تحميل manifest.json
   ↓
5. تحميل جميع الأجزاء بالتوازي
   ↓
6. دمج الأجزاء
   ↓
7. تحميل النموذج في ONNX Runtime
   ↓
8. جاهز للاستخدام ✓
```

### 2. معالجة النص إلى صوت

```
1. المستخدم يدخل النص
   ↓
2. التحقق من صحة الإدخال
   ↓
3. استدعاء piper.synthesize()
   ↓
4. معالجة النص بواسطة ONNX Model
   ↓
5. إنشاء Audio Buffer
   ↓
6. تحويل إلى WAV Blob
   ↓
7. تشغيل الصوت
   ↓
8. عرض خيارات التحميل
```

## 💾 إدارة الذاكرة

### استهلاك الذاكرة

| المرحلة | الاستهلاك |
|--------|----------|
| تحميل الأجزاء | ~140 MB |
| تحميل النموذج | ~300 MB |
| معالجة النص | ~50 MB |
| **الإجمالي** | **~500 MB** |

### التحسينات المطبقة

1. **تحميل الأجزاء بالتوازي**:
```typescript
Promise.all(chunks.map(chunk => fetch(chunk)))
```

2. **تحرير الذاكرة بعد الاستخدام**:
```typescript
URL.revokeObjectURL(audioUrl)
```

3. **استخدام Typed Arrays**:
```typescript
const buffer = new Float32Array(data)
```

## 🚀 الأداء

### معايير الأداء

| المقياس | القيمة |
|--------|--------|
| وقت التحميل الأول | 1-2 دقيقة |
| وقت المعالجة (نص قصير) | 2-5 ثواني |
| وقت المعالجة (نص طويل) | 10-30 ثانية |
| حجم التطبيق (بدون نموذج) | ~50 KB |

### تحسينات الأداء

1. **التخزين المؤقت (Caching)**:
```typescript
const cache = await caches.open('piper-v1')
cache.put(modelUrl, response)
```

2. **ضغط الملفات (Compression)**:
```bash
gzip -9 public/models/*.onnx
```

3. **التحميل الكسول (Lazy Loading)**:
```typescript
// تحميل النموذج عند الحاجة فقط
if (!modelLoaded) {
  await loadModel()
}
```

## 🔐 الأمان

### إجراءات الأمان المطبقة

1. **التحقق من صحة الملفات**:
```typescript
// التحقق من حجم الملف
if (file.size > MAX_SIZE) {
  throw new Error('File too large')
}
```

2. **CORS Headers**:
```typescript
headers: {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'GET',
}
```

3. **Content Security Policy**:
```html
<meta http-equiv="Content-Security-Policy" 
      content="default-src 'self'; script-src 'self' 'wasm-unsafe-eval'">
```

## 🧪 الاختبار

### أنواع الاختبارات

1. **اختبارات الوحدة (Unit Tests)**:
```typescript
describe('PiperTTSApp', () => {
  it('should load model successfully', async () => {
    // ...
  })
})
```

2. **اختبارات التكامل (Integration Tests)**:
```typescript
describe('Text to Speech', () => {
  it('should synthesize text correctly', async () => {
    // ...
  })
})
```

3. **اختبارات الأداء (Performance Tests)**:
```typescript
describe('Performance', () => {
  it('should load model within 2 minutes', async () => {
    // ...
  })
})
```

## 📈 التوسع المستقبلي

### ميزات مخطط لها

1. **دعم لغات إضافية**:
   - العربية
   - الفرنسية
   - الإسبانية

2. **نماذج إضافية**:
   - LibriTTS Medium
   - LibriTTS Fast
   - نماذج مخصصة

3. **ميزات متقدمة**:
   - تحرير الصوت
   - تطبيق المؤثرات
   - دعم الوقت الفعلي

4. **تحسينات الأداء**:
   - WebAssembly optimization
   - GPU acceleration
   - Streaming synthesis

## 🔗 المراجع التقنية

- [ONNX Runtime Web](https://github.com/microsoft/onnxruntime/tree/main/js/web)
- [Piper TTS](https://github.com/rhasspy/piper)
- [Web Audio API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API)
- [Cloudflare Workers](https://developers.cloudflare.com/workers/)

---

**آخر تحديث**: January 2026
